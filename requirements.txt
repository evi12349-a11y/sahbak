# סחבק — Python dependencies
# ---------------------------------------------------------------------------
# IMPORTANT: google-genai must be recent enough to support BOTH
#   • parameters_json_schema= on FunctionDeclaration, and
#   • ThinkingConfig(thinking_budget=...)
# The code falls back gracefully if these aren't available, but on an old
# google-genai the thinking-budget=0 fix (the main bug fix) can't apply and
# you'd still see the "200 OK but empty" behaviour. Keep this pinned & fresh.

google-genai>=1.16.0
google-api-python-client>=2.130.0
google-auth>=2.30.0

flask>=3.0.0
gunicorn>=22.0.0
requests>=2.32.0
Flask-Cors==4.0.1
# Timezone database — required for ZoneInfo('Asia/Jerusalem') on slim
# Linux images (Railway/Nixpacks). Without it, now_local() falls back to UTC
# and every "tomorrow at 9", month rollover, and timestamp is 2-3h off.
tzdata>=2024.1
